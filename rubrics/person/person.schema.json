{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "one person, judged at one year",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "person_period_id", "judged", "reasoning"],
  "properties": {
    "schema_version": {"const": "person-1.0"},
    "person_period_id": {"type": "string"},
    "judged": {"type": "boolean"},
    "cannot_judge_reason": {
      "type": ["string", "null"],
      "enum": [null, "person_unknown", "date_unplaceable", "other"]
    },
    "score": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 10,
      "description": "Conventional attractiveness as perceived in that year. null when judged is false."
    },
    "evidence_used": {
      "type": ["boolean", "null"],
      "description": "True when a supplied dated observation informed the score."
    },
    "evidence_ids": {
      "type": "array",
      "items": {"type": "string"},
      "description": "The supplied observation ids this score rests on. Empty when none were supplied or none were used."
    },
    "reasoning": {"type": "string", "minLength": 1}
  }
}
